package r5js;

import java.io.IOException;

public final class Main {

    public static void main(String[] args) throws IOException {
//        String result = new SchemeEngineBuilder().build(Platform.NODE).compiled;
//        Files.write(Paths.get("build", "test-all.js"), result.getBytes());
//        System.exit(0); // to avoid annoying exec-maven-plugin pause
    }
}
