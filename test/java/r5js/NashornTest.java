package r5js;

public final class NashornTest {

    /**
     * This is disabled because it doesn't work prior to java -version 1.8.0_40
     * When it does work, it's unbearably slow, much slower than shelling out to Node.
     @Test public void testPassthrough() {
     Application.launch(R5JSTests.class);
     }
     */
}
